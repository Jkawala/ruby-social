<!DOCTYPE html>
<html>
  <head>
    <title>Social</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <!-- A div box to highlight webpage notification-->
    <% if flash[:notice] %>
    <div class="notification is-primary global-notification">
      <p class="notice"><%= notice %></p>
    </div>
      <% end %>

      <!-- A div box to show alerts if there is a problem with the users account --->
      <% if flash[:alert] %>
      <div class="notification is-danger global-notification">
      <p class="alert"> <% alert %></p>
      </div>
      <% end %>

<!-- navbar/ header of the website for all pages-->
     <nav class="navbar is info" style="background: #dabdad;">
     <div class ='navbar-brand'>
      <%= link_to root_path, class: "navbar-item" do %>
      <h1 class="title is-5">JK Blogs</h1>

       <% end %>

         <div class="navbar-burger" data-targert="navbarExample">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
          <!-- %= render "search/form" %> -->
             <a class="btn btn-light action-button" role="button" href="#" style="background: #d49a6a;">Account</a>
     </div>
    </div>

<!--mobile version of the website-->
    <div id="navbarExample" class="navbar-menu">
      <div class="navbar-end">
        <div class="field is-grouped">
          <p class="control">
          <%= link_to "New post", new_tweet_path, class: "button is-info is-inverted" %>
          </p>
          <p class="control">Sign Up</p>
        </div>
      </div>
    </div>
 </nav>

    <%= yield %>
  </body>
</html>
